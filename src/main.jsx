import React from 'react'
import ReactDOM from 'react-dom/client'
import LeetFlash from './LeetFlash.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LeetFlash />
  </React.StrictMode>,
)
